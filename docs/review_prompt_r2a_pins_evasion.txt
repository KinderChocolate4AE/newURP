================================================================================
R2A 고정 파라미터 × 표적 기동회피 수준 — 외부 감사 브리프 r1 (2026-09-05)
선행 문서: review_prompt_r2a_parameterization.txt (r3 + r4 부록 §10).
봉인 커밋 a9fee8c (hash 3종) + a016587 (ledger inject 확장, hash 불변) 이후,
Stage 1 착수 전. 이 감사가 닫히기 전 Stage 1 에피소드를 생성하지 않는다.
================================================================================

0. 문서 지위 / 감사 요청 범위
--------------------------------------------------------------------------------
실행 전 감사. 쟁점 2건이 결합되어 있어 한 문서로 묶는다:
  쟁점 1 — 봉인 world 선언("threat A1")과 Stage 0 원천 데이터(A2-reactive)의
           표적 기동회피 수준 불일치. 재봉인 여부 결정 필요.
  쟁점 2 — 고정(pin) 파라미터 전수 인벤토리의 치명도 분류 — 어느 고정값이
           판정(내부 타당성)을 깨고, 어느 것이 scope(외부 타당성)만 좁히는가.
요청: §5 수치 주장의 적대적 재검증 + §6 질문에 대한 판정. 결과 데이터는 아직
없다 — 설계 감사이며, 재봉인은 실행 전이므로 허용된다는 것이 우리 입장이다
(이 입장 자체도 감사 대상, Q1).

1. 현재 봉인 상태 (요약)
--------------------------------------------------------------------------------
  계약 2종 (pooling 금지):
    R2a-L  a∈[11,78], tau_B 0.375, directional only   hash a854a57a643a17fd
    R2a-P  a∈[6,78],  tau_B 0.45,  confirmatory        hash e43036f00679ff77
    Stage 0 envelope (exploratory)                      hash 3878260e937f9b05
  margin: delta_p 0.10 / delta_chi 0.05 (별도 estimand; slope 역산 폐기).
  격자: map chi 0.41…0.85 step 0.04 × eta 2.1…3.9 step 0.3 (R-ref 전용) /
        boundary micro-grid chi50±0.10 step 0.02 (11점).
  a_min 규칙: H_support ≥ max(delta_chi, 2·step_micro) = 0.05.
    후보 7 → 게이트가 tau 0.425 로 강등, H +0.043, reject.
    후보 6 → tau 0.45, H +0.057, accept.
  Stage 0 판독: chi50(eta) 0.598→0.541 (on-lattice), pooled isotonic 0.565,
    transition step-like (chi<0.49 p≈1 / >0.73 p=0). censored 0.

2. 쟁점 1 — 표적 기동회피 수준 불일치 (봉인 vs 데이터)
--------------------------------------------------------------------------------
2.1 사실관계 (실측):
  - Stage 0 원천 = results/curve_hold_reactive.json 헤더: mode hold, level=A2,
    jink_amp=0.6, w_kill=0.5, n=2,700.
  - 그러나 route_gain / sense_range 필드가 헤더에 **없다** (R-007 payload 통일
    이전 포맷). route_gain 0.5 · sense_range 30.0 귀속의 근거는 ① 파일명
    "reactive" ② 2026-08-13 gate11 노트 "본실행: route_gain 0.5 · sense_range
    30.0" ③ results/README 계보 라벨 — 즉 아티팩트 내장 증거가 아니라 문서
    증거다. **provenance 등급: 문서-추정** (Q2).
  - 봉인 lattice 의 world 문자열 = "threat A1". docs/87 §4 = "적대자 = A1 단일
    (grid 전면)". 즉 봉인 선언과 Stage 0 데이터의 표적 수준이 어긋난다.
2.2 왜 치명적인가: boundary micro-grid 는 Stage 0 의 A2-reactive 경계
  chi50(eta) 를 중심으로 ±0.10 이다. Stage 1 을 A1(jink 0, route 0) 로 돌리면
  회피가 꺼진 표적의 경계는 더 높은 chi 에 있을 개연성이 크고(회피가 포획을
  깎는 방향), 새 경계가 W=0.10 밖이면 micro-grid 전체가 헛 위치 → crossing
  censored → tau_B 게이트·H 규칙이 전제한 identifiability 가 즉시 붕괴한다.
  이는 r3 §2 가 경고한 "동방향 censoring 에 의한 D_chi 계산 불능" 의 자충수
  버전이다.
2.3 제안 D-2 (권고): R2a 전 Stage 를 **A2-reactive (jink 0.6, route 0.5,
  sense 30)** 로 통일. world 문자열·docs/87 §4 문구 수정 후 재봉인 (새 hash 3종,
  실행 전이므로 결과 의존 없음). 장점 = Stage 0 envelope·KSAS 곡선과 같은 세계,
  Stage 0 재실행 0비용. 단점 = "A1 단일" 선언과의 문서 정합 수정 필요.
  대안 D-2': A1 유지 → Stage 0 을 A1 로 신규 실행 (2,700판 × 1.49 s/ep ≈ 67분,
  ×1.5 안전 ≈ 100분 — 30분+ 규칙상 랩서버, H-4 선행). envelope·게이트 전부 재산.
  대안 D-2'' (비권고): L=A1 / P=A2 혼합 — 계약 간 비교 자체가 무의미해짐.
2.4 부속 제안 (Q2): 어느 쪽이든 route_gain 귀속을 아티팩트 증거로 승격 —
  frozen 과 동일 (seed0=0, ep 0~49) 50판을 route_gain 0.0 과 0.5 로 각각
  재실행해 라벨 일치율로 판별하는 bit-exact spot-check (로컬 ~2.5분). 판별
  결과를 sidecar 로 기록 (원본 JSON 소급 수정 금지).

3. 쟁점 2 — 고정 파라미터 전수 (코드 실측) 와 치명도 3층
--------------------------------------------------------------------------------
3.1 고정값 표 (R2a-P 기준; τ→0.45 / ρ→2.30 교란 시 target 외 전부 co-scale):

  [시간]   dt 0.05 · tau_lock 0.10 · tau_kill 0.15        → dt/τ 1/6, 1/3, 1/2
  [길이]   r_kill 0.75 · R_max 8.22 · R_NK 6.0            → κ 0.4237, λ 4.644
           layout: ring_center 8.0 / ring_radius 5.0 / r_ring 2.1 /
           finisher 2.0 / x0 24.0 / x_fire 16.0 / target_r 1.0   (전부 /ρ pin)
  [스폰]   dx 2.0 · r_lat 5.0                              → 1.130, 2.825
  [능력비] a_lim=0.35·a_att (μ) · v_lim=1.0·v_att (ν) · v_adv,max=1.5·v_att
  [gain]   k_f 4.0 [1/s] (→k_f·τ 1.20, R-tau-DOM target) · ω_aim 2.0 [1/s]
           (★M4 override — params 기본 3.14 아님) · backend slew 10.0 [1/s]
           (★frozen env 의 8.0 은 DEAD 인자 — params 레지스트리 명시) ·
           limiter_ω 2.5 · kp 8.0 / kd 4.0
  [표적 행동 — A2-reactive] jink_amp 0.6 (무차원 ×a_lat) · jink_freq 1.5 [1/s]
           (→jink_freq·τ 0.45) · jink_terminal_r 3.0 [m] · homing_gain 4.0
           [1/s] · route_gain 0.5 · sense_range 30.0 [m] (→σ_dt 16.95) ·
           dodge_amp 1.8 (commit 후, 무차원) · repel_margin 1.0 · react True ·
           bait/sprint/slowdown 전부 0 (inert)
  [판정]   commit_threshold 0.5 · p_kill 1.0 · w_kill 0.5 · hold arm ·
           capture_terminates · episode 160 step (→T/τ 26.7, dt co-scale 로 불변)

3.2 층 1 — 봉인-실제 불일치 (즉시 수정 대상): 쟁점 1 의 A1/A2 가 유일 잔존.
  같은 부류였던 ω_aim (2.0 vs 3.14) 과 slew (10.0 vs dead 8.0) 는 a016587 에서
  ledger 주입값으로 정정 완료 — 감사는 이 정정의 정합만 재확인하면 된다.
3.3 층 2 — 내부 타당성 (co-scale 누수만이 판정을 깬다): H-SIM/H-DOM 은 "차원값
  을 바꿔도 무차원 지도가 불변인가" 만 검정하므로, 고정 무차원값이 무엇이든
  판정 자체는 성립한다. 깨지는 유일 경로 = 어떤 차원 상수가 co-scale 에서
  빠져 π 가 몰래 움직이는 것. 현재 ledger inject 16종: dt · tau_lock ·
  tau_kill · ω_aim · ω_slew · k_f · jink_freq · homing_gain · jink_terminal_r ·
  sense_range · R_max · dx · r_lat · x0 · r_kill (+ρ, τ 자체). 무차원군 16개가
  주입값에서 계산되어 SIM 전부 invariant / DOM target 만 이동을 테스트가
  assert 한다 (tests/test_r2a.py 11종).
  ★감사 질문 Q3: 이 목록의 완비성. 우리가 의도적으로 뺀 것 = 판정·수치 상수
  (witness n=2000/n_segments 4/n_dir 32/n_t 24, θ 0.9, eps 1e-6 — 길이/시간
  차원이 아니라 판정 해상도), layout 나머지 (ring/x_fire/target_r — /ρ pin 으로
  ledger 밖), episode_len (스텝수 — dt co-scale 로 T/τ 불변). 이 배제가 옳은가.
3.4 층 3 — 외부 타당성 (scope): 지도의 정직한 이름은
  "고정 표적 기동회피 행동비 (A2: jink 0.6 · route 0.5 · σ_dt 17) + 고정 기하비
   + capability-ratio family (μ 0.35, ν 1.0) 의 hold-arm net 포획 지도"
  다. C046 caveat 문구를 이대로 개정 제안 (Q5). 경계 위치를 실제로 움직일
  고정값 순위 주장 (Q6, 적대 검증 요청):
    jink_amp·route_gain (회피 강도 — 경계 높이 주 결정자, self-play 트랙 ③의
    소관) > k_f·τ (R-tau-DOM 이 시험) > λ/α (R-rho-DOM 이 시험) > κ 0.424 >
    commit_threshold. hold arm 관성(inert) 주장: μ·ν·kp/kd·limiter_ω·finisher
    한계 — limiter 가속 0·finisher 정지이므로. 경험 근거: frozen 2,700판 라벨
    = NET_CAPTURE 458 / PENETRATED 2,242, **HARD_KILL 0** → hard-kill competing
    risk 부재가 in-sample 로 확인됨. 단 이는 기준 구현 한정 — 교란 구현에서
    hard-kill 이 깨어나면 μ 가 estimand 에 개입한다. Stage 1 에 "라벨 분포에
    HARD_KILL 출현 시 STOP-and-classify" 게이트 추가 제안 (Q4).

4. 이 브리프가 세운 자기의심 (감사가 공격할 지점)
--------------------------------------------------------------------------------
  - "A1 로 돌리면 경계가 위로 간다" 는 방향 추정이지 실측 아님 — D-2' 선택 시
    실측으로만 판정.
  - route_gain 0.5 귀속은 문서 증거뿐 (§2.1). spot-check 전까지 확정 금지.
  - jink_freq 1.5 의 단위·위상 규약([1/s] sinusoid)은 코드 주석 기반 — 감사에서
    jink_freq·τ 가 올바른 무차원화인지 (2π 인자 포함 여부) 확인 요망.
  - "μ inert" 는 hold arm + 기준 구현의 경험적 사실이지 구조적 증명 아님 (§3.4).
  - inject 는 선언이지 주입 실증이 아님 — 하네스가 16종을 실제로 받는지는
    Stage 1 착수 시 ledger 실현성 검증 (r4 §10) 이 별도로 확인한다.

5. 수치 주장 (적대적 재검증 요청 — 전부 이 세션 실측)
--------------------------------------------------------------------------------
  N-1  support: chi_min(τ0.45, a11)=0.629 / (τ0.45, a7)=0.400 / (τ0.45, a6)=0.343
  N-2  a_min 게이트: 7 → tau 0.425·H+0.043 reject / 6 → tau 0.45·H+0.057 accept
  N-3  chi50(eta) on-lattice 0.598…0.541, pooled 0.565, boot CI 반폭 0.02~0.07
  N-4  κ=0.75/1.77=0.4237 · λ=8.22/1.77=4.644 · α=arctan(1/λ)=12.15° ·
       R-rho-DOM target λ 3.574 / α 15.63°
  N-5  무차원 시간군: dt/τ 0.1667 · τ_lock/τ 0.333 · τ_kill/τ 0.5 · k_f·τ 1.20 ·
       ω_aim·τ 0.60 · slew·τ 3.0 · jink_freq·τ 0.45 · homing·τ 1.20 · T/τ 26.67
  N-6  frozen 라벨: 458/2242/0 (NET_CAPTURE/PENETRATED/HARD_KILL), n 2700
  N-7  hash: stage0 3878260e937f9b05 · L a854a57a643a17fd · P e43036f00679ff77
  N-8  A1 파일럿 비용 (D-2' 시): 2,700×1.49s ≈ 67분, ×1.5 ≈ 100분 (랩서버)

6. 감사 판정 요청 (결정 순서대로)
--------------------------------------------------------------------------------
  Q1  실행 전 재봉인(D-2, world=A2-reactive + 새 hash 3종)의 사전등록 적법성.
      우리 입장: 에피소드 0개 생성 상태이므로 결과 의존이 원천 불가 — 허용.
  Q2  route_gain provenance 승격안 (§2.4 spot-check) 의 충분성. 또는 더 강한
      요구 (예: 2,700판 전체 재실행 대조) 가 필요한가.
  Q3  co-scale inject 16종의 완비성 + §3.3 배제 3부류의 정당성.
  Q4  Stage 1 HARD_KILL 출현 STOP 게이트 문안 채택 여부.
  Q5  estimand/C046 개정 문구 (§3.4) 승인.
  Q6  치명도 순위 (§3.4) 의 반례 — 특히 "회피 행동비가 경계 높이의 주 결정자"
      주장이 R2a 결론의 어휘 (consistent with (chi,eta) dominance) 를 추가로
      좁혀야 하는지 (예: "…under the declared evasion behavior ratios").
7. 감사 반영 기록 r2 (2026-09-05 — 판정: 조건부 재봉인 승인, blocker 3건)
--------------------------------------------------------------------------------
  판정 요약: Q1 승인 (논거 교정) · Q2 조건부 충분 (exact replay 승격) · Q3 불완전
  → 보완 · Q4 채택 (강화 문안) · Q5 방향 승인 (문구 일반화) · Q6 기각 (순위 강등).

  반영 내역:
  (1) Q1 논거 교정: "결과 의존 원천 불가" 삭제. 올바른 논거 = Stage 0 는 선언된
      exploratory 설계 데이터이고, result-informed design 은 이미 (허용된 형태로)
      일어났다 — 재봉인이 의존해선 안 되는 것은 confirmatory (Stage 1+) 결과이며
      그것이 0개다. supersession lineage: 구 seal (a9fee8c 의 hash 3종) 보존 →
      D-2 superseded 기록 → 새 seal. SUPERSEDES 블록으로 기계 기록.
  (2) Q2 해소 — 2단 증거로 승격:
      1차 (runtime lineage, 감사가 "가장 강한" 으로 지정한 형태): 생성 커밋 43acc39
      가 동봉한 sidecar `results/curve_hold_reactive.manifest.json` 이 route_gain
      0.5 · sense_range 30.0 · jink 0.6 · run commit edf34d9 · campaign id 를 기록.
      r1 의 "문서-추정" 등급은 과했다 (자기정정) — 이는 실행 세션이 남긴 기계
      sidecar 다.
      2차 (exact replay 검증): `r2a_provenance.py` — 경계 밴드 (chi 0.45~0.75) 60판
      을 후보 (0.5/30) vs 대조 (0.0/inf) 로 재생. 비교 순위 = 결정론 2회 재현 (연속
      metric 포함 완전 일치) > config 간 metric 분기 (min_target_dist·steps) >
      위협 draw bit-exact > terminal label. CONFIRMED 규칙 = 후보 라벨 전수 일치
      AND 결정론 AND metric 분기 존재 AND 대조가 전수 일치는 아님. 라벨 단독
      판정 아님 (감사 지적 수용). 재봉인은 verdict=CONFIRMED 를 assert 로 요구.
  (3) Q3 보완: "내부 타당성을 깨는 유일 경로 = co-scale 누수" 문장 철회. 계약을
      2층으로 재기술 — ① 차원량 co-scale (inject 22종: 기존 15 + layout 7:
      ring_center 8 / ring_radius 5 / r_ring 2.1 / finisher 2 / x_fire 16 /
      target_r 1 / R_NK 6) ② 무차원 conditioning 상수의 정확 불변 (CONDITIONING_
      VECTOR). runtime_norm 블록 (/rho 7종) 은 DOM 포함 전 구현 invariant 를 기계
      assert (누수 주입 시 거부 테스트 포함). 잔여 경로 (RNG/IC law · event
      ordering · 이산화) 는 Stage 1 의 pathwise·dt-수렴 gate 소관으로 명시.
      eps 감사: R2a runtime 경로의 eps 는 전부 _EPS=1e-12 norm guard — 의미상
      차원 인접이나 물리 크기 대비 12~13 자릿수 여유로 numerically inert 분류,
      co-scale 하지 않음 (EPS_AUDIT 블록에 위치·분류 기록). r1 의 "eps 1e-6" 은
      c1_* 스크립트대 상수로 R2a 경로 밖 (판정 해상도 상수).
  (4) Q4 채택 (감사 문안): "hold-arm confirmatory run 에서 HARD_KILL 1회 발생 시
      해당 batch 의 H-SIM/H-DOM 판독 중지, competing-risk emergence 로 분류. 원인
      확인 전 NET_CAPTURE 분모에서 삭제·재코딩 금지." STAGE1_GATES 로 봉인.
  (5) §8 반영: "mu inert" 표현 전면 삭제. mu 는 운동학적으로 hold arm 에서 관성
      이나 commit margin 식 r_c + 0.5(a_lim−a_att)·tau_kill² 을 통해 결정 논리에
      active — 고정 conditioning ratio 로 유지. nu/kp/kd/limiter_omega 도 "코드
      경로 dead 확인 전 inert 표기 금지" 규율 적용.
  (6) Q5: estimand 를 "the hold-arm net-capture map conditional on the sealed
      A2-reactive evasion vector, normalized geometry/controller constants, and
      fixed capability ratios" 로 일반화. C046 은 숫자 목록 복붙 대신 sealed
      configuration hash 를 정본 reference (provenance 닫힌 뒤 = 지금 충족).
  (7) Q6: 치명도 순위 철회 → A_PRIORI_SENSITIVITY_CANDIDATES (비서열 후보 목록)
      로 강등. 전역 어휘 협소화 채택: "consistent with (chi, eta) dominance under
      the declared perturbations, **conditional on the sealed A2-reactive evasion
      behavior vector**" — jink/route 축은 R2a 가 시험한 적 없음을 어휘에 반영.
  (8) jink_freq 표기: 코드 = sin(2*pi*f*t + phi) → f*tau = 0.45 는 "tau 당 cycle
      수", canonical phase advance = 2*pi*f*tau = 2.827 rad. SIM 불변성에는 무영향,
      논문 표기 규약으로 CONDITIONING_VECTOR 에 명기.
  (9) docs/87 §4 "적대자 = A1 단일" 을 D-2 개정 (날짜·근거 명기, 소급 아님).

  재봉인: stage0/lattice 스키마 v3 — world = A2-reactive + supersedes + conditioning
  vector + evasion_provenance + eps_audit + stage1_gates 포함 후 새 hash 3종 (본
  문서의 r2 부록 말미에 기록). 이후 순서: repo-R1 → H-4 → Stage 1.
8. 재봉인 결과 (2026-09-05 — blocker 3건 전부 닫힘)
--------------------------------------------------------------------------------
  provenance spot-check (r2a_provenance.py, 경계 밴드 60판 + 결정론 10판×2):
    verdict CONFIRMED — 후보 (0.5/30) 라벨 60/60 frozen 일치 · 대조 (0.0/inf)
    54/60 (판별 6판 전부 후보 승) · 결정론 완전 재현 · 위협 draw bit-exact 60/60 ·
    config 간 metric 분기 60/60. → artifacts/r2a/provenance_route_sense.json
  새 seal (schema v3 — world A2-reactive + supersedes + conditioning_vector +
  evasion_provenance + eps_audit + stage1_gates):
    Stage 0  4c26cf1a2a4d9ab8   (구 3878260e937f9b05 supersede)
    R2a-L    da36d96eb5bceddc   (구 a854a57a643a17fd supersede)
    R2a-P    3aa3adef77420d12   (구 e43036f00679ff77 supersede)
  envelope·게이트 수치 전부 불변 (a_min 6 / tau_B 0.45 / H +0.057 / L directional).
  ledger: inject 22종 + pi 16군 + runtime_norm 7종 (전 구현 invariant, 누수 거부
  테스트 포함). 테스트 19/19. 이후: repo-R1 → H-4 → Stage 1 (STAGE1_GATES 준수).
================================================================================
