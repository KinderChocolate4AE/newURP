# 역할 분리 2×2 — 사전등록 + 배선

**2026-08-04 · docs/47 §7 의 다음 단계. 학습 결과를 보기 전에 쓴다**

---

## 0. 요약

```
문제        LL(둘 다 학습)이 SS(hold)보다 나쁜데, 그 이유를 두 가설로 못 가른다
팔          limiter {학습, hold} x finisher {학습, 스크립트} = 2x2, SS 는 기존 기저선
판정        SHAPING 무력화율. docs/47 §4.3 그대로 -- **바꾸지 않는다**
배선        역할 동결이 학습 롤아웃·평가 양쪽에 걸린다. 스크립트 규칙은 기저선과 공유
새 테스트   P50~P60 (22건). 총 481 passed, 4 skipped
비용        15 런 (3 팔 x 5 시드). 파일럿과 같은 500k 스텝. 1런 4~5시간(실측)
★ 스모크    2048 스텝 배선 확인에서 LS 가 기저선의 EASY 성적을 재현했다
            (네트 0.833 vs 기저 0.832) -- **배선 검증이지 결과가 아니다**
```

---

## 1. 왜 필요한가 — 기준선이 비어 있다

파일럿(docs/47 §7)에서 학습은 `hold` 보다 나빴다. 그런데 그 사실로부터
알고리즘에 대해 말할 수 있는 것이 없다. `hold` 가 무개입이 아니기 때문이다:

```
발사 off (fire_mode="never")      무력화 0.000     <- 진짜 무개입
hold                              무력화 0.182     <- 편대는 무개입, 발사는 해석적 규칙
학습 (파일럿 3런)                 무력화 0.023~0.080
```

`hold` 의 0.182 는 **전부 발사 규칙의 성적**이다. 정적 링의 v_shot 기여도는
531 스텝 중 94.9 % 가 완전 동일하다(docs/47 §7 재검증). 그러므로

> LL < SS 는 "협력 성형을 못 배웠다" 와
> "학습된 발사 타이밍이 해석적 규칙보다 나쁘다" 를 구분하지 못한다.

두 가설은 다음 작업이 완전히 다르다. 전자면 보상·탐색(PBRS·커리큘럼)의 문제이고,
후자면 발사 헤드의 확률 붕괴(파일럿 s1 은 p=0.0002, s0 는 p=0.9999)를 먼저 잡아야
한다. 역할을 하나씩 고정하면 갈린다.

---

## 2. 팔 (선언)

| 팔 | limiter | finisher | 읽는 것 |
|---|---|---|---|
| **SS** | `hold` | 스크립트 (clean 임계) | 기준선. **학습 없음** — `results/hold_baseline.json` (n=500) |
| **LS** | 학습 | 스크립트 (clean 임계) | 발사를 고정했을 때 **편대 학습**의 단독 기여 |
| **SL** | `hold` | 학습 | 편대를 고정했을 때 **발사 학습**의 단독 기여 |
| **LL** | 학습 | 학습 | 결합 (= 파일럿 구성, 결함 수정 후 재측정) |

축은 이것뿐이다. `w_kill = 0.5` 로 **고정**한다 — 여기서 묻는 것은 "어느 역할이
격차를 만드는가" 이지 "어떤 보상 가중치가 좋은가" 가 아니다. 시드 {0, 1, 2, 3, 4}.

시드를 5개로 두는 이유는 검정력이 아니라 **시드 분산**이다. 파일럿에서 행동
스케일이 에피소드 0 값으로 동결돼 있었고 그 때문에 시드마다 실효 권한이
100/72/48 % 로 갈렸다 — *"기존 3시드는 복제가 아니다"* (docs/47 §7 재검증).
결함은 고쳤지만, 3개로 시드 분산을 추정하는 것은 여전히 사실상 불가능하다.
(선언 시점: 2026-08-04, 학습 결과 전.)

`SS` 를 다시 돌리지 않는 이유: 학습기를 거치지 않으므로 파일럿의 세 결함
(관측 오염·소모 벌점·행동 스케일)이 닿지 않는다. docs/47 §7 이 "기준선 두 줄은
유효하다" 고 적은 그 근거다. 스윕과 **같은 파일**을 쓰므로 두 실험의 기준선이
동일하다.

### 2.1 동결은 학습 시점이다 (평가 시점 스왑이 아니다)

이미 학습된 LL 체크포인트에서 평가 때만 finisher 를 스크립트로 바꾸는 방법도
있다. 그건 **하지 않는다**. 그 값은 "짝으로 학습된 정책을 떼어놨을 때의 조합
가능성"이지 "그 역할이 혼자 무엇을 배울 수 있는가"가 아니다. 귀속을 물으려면
각 팔이 **자기 조건에서 학습**해야 한다.

대가는 명시한다: LS 의 limiter 는 *고정된 발사 규칙을 상대로* 최적화되므로
LL 의 limiter 와 같은 문제를 푸는 것이 아니다. 그래서 §4 의 차분은 "역할의
한계 기여"가 아니라 **"그 역할만 학습시켰을 때의 도달점"** 이다. 상호작용 항을
따로 내는 것이 그 때문이다.

---

## 3. 배선

### 3.1 한 곳 원칙

동결된 역할의 스크립트 규칙은 기준선의 그 역할과 **같은 함수**여야 한다.
갈라지면 LS−SS 차이가 "학습의 기여"가 아니라 "두 구현의 차이"가 된다.
그래서 `mission_rollout.scripted_role_actions()` 하나만 둔다 — 기준선 경로와
동결 경로가 이것을 공유한다.

docs/47 §7.4 가 적은 대로 이 리포에서 반복된 사고(정정 3 · 정정 8 · 결함 1 ·
결함 2)는 전부 *"같은 규칙이 두 곳에 있고 소비자 한쪽이 안 따라왔다"* 였다.
이번에는 문서가 아니라 **테스트**로 못을 박는다 (§3.4).

### 3.2 두 경로에 같이 걸린다

```
학습 롤아웃   MAPPORunner.collect_rollout -> _override_live() 훅 (기본 항등)
              M4Runner 가 오버라이드해 동결 역할을 스크립트로 갈아끼운다
평가          mission_eval(scripted_roles=...) -> run_episode(scripted_roles=...)
              정책이 무엇을 내든 그 역할만 스크립트로 덮어쓴다
```

부모 `collect_rollout` 을 자식이 복사하지 않도록 **훅 두 개**(`_override_live`,
`_observe_step`)만 넣었다. 기본 구현이 항등/no-op 이므로 M2/M3/2B/2C 경로는
bit-identical (P56).

패딩 규약 주의: 스크립트는 **env Box**(limiter 4, finisher 5)를 내고
`adapter.step` 은 **LIVE** 차원을 받는다. `unpad_env_action` 이 유일한 변환
지점이다 — 다른 데서 하면 프로파일(`M4_LIVE_DIMS`)이 갈라져 결함 2 가 재발한다.

### 3.3 동결된 액터는 학습에서 뺀다

행동이 env 에 닿지 않는 액터를 그대로 학습시키면 두 경로로 오염된다:

1. `clip_grad_norm_` 이 **전역 노름**이라, 동결 액터의 grad 가 살아있는 액터의
   스텝 크기를 줄인다.
2. `target_kl` 조기중단이 두 KL 의 `max` 라, 동결 액터가 살아있는 액터의
   epoch 을 끊는다.

그래서 `MAPPOConfig.freeze_limiter / freeze_finisher` 를 두고, 동결 역할의
forward 를 `torch.no_grad()` 로 돌려 **그래프 자체를 안 만든다**. (0 을 곱하는
것만으로는 grad 텐서 0 이 생겨 Adam 의 모멘텀이 파라미터를 움직인다.)
기본값은 둘 다 `False` 이고, 그때 업데이트는 이 배선 이전과 파라미터까지
동일하다 (P55).

부수: limiter 가 동결된 팔에서 `limiter/commit_rate` 는 **제안**이지 일어난
일이 아니므로 키 이름을 `limiter/commit_rate_proposed` 로 바꾼다. 같은 키로
내면 표에서 구분되지 않는다.

### 3.4 테스트 (P50–P60, 22건)

| ID | 지키는 것 |
|---|---|
| P50 / P50b | `unpad(pad(a)) == a`. `pad(unpad(x))` 는 RESERVED 를 0 으로 만든다 (비대칭 명시) |
| P51 / P51b | 스크립트 역할 행동의 모양·부분호출 동치. `hold` 는 가속 0 **이고 커밋 0** |
| **P52** | ★ 두 역할 동결 == 손튜닝 기준선 (결과 필드 11개 전부 동일) |
| P52b | 부분 동결에서 살아있는 역할은 정책 값이 그대로 env 에 닿는다 |
| P53 / P53b | 동결 limiter 가 **env 에 닿는 값**이 0. `scripted_roles=()` 는 기존 경로와 동일 |
| P54 / P54b | 동결 액터는 update 후 **정확히** 불변, 살아있는 쪽은 변한다. 크리틱은 항상 학습 |
| P55 | freeze 기본값 False + 그때 파라미터까지 bit-identical |
| P56 | 부모 훅이 항등/no-op |
| P57 | 두 역할 동시 동결 거부 (CLI·학습기 양쪽) |
| P58 | `ARMS` 키 집합 == CLI 2×2 − SS |
| P59 / P59b / P59c / P59d | 판정이 선언 규칙 그대로. '강함' 은 시드 **과반**(상수 아님). 선언 시드 축 = 5 |
| P61 / P61b | 실행 풀이 `jobs` 를 **넘겨 띄우지 않는다**(sweep_m4 루프는 순간 `jobs+1`). 런별 로그 분리. 한 런이 죽어도 나머지는 완주 |
| P62 / P62b / P62c | 알림은 토픽 없으면 no-op, 네트워크가 죽어도 예외를 안 낸다. `train_m3a` 가 사본이 아니라 단일 정의를 부른다. 푸시 본문에 판정이 실린다 |
| P60 / P60b | null case 도 판정으로 나온다. **기저선이 없으면 판정하지 않는다** |

P52 에는 **음성 대조**가 붙어 있다. 같은 쓰레기 정책을 동결 없이 넣으면 결과가
달라져야 한다 — 이게 없으면 "동결이 먹혔다"와 "이 판은 무엇을 해도 똑같다"가
구분되지 않아 테스트가 조용히 공허해진다. 실제로 `hold` 로 침투당하기만 하는
판(ep 0/3/7)은 쓰레기 정책을 그대로 넣어도 결과가 안 바뀐다 — 그래서 발사가
일어나는 판(ep 1/2/4/10)으로 고정했다.

### 3.5 파일

| 파일 | 변경 |
|---|---|
| `shepherd/train/action_dims.py` | 신규 `unpad_env_action` (패딩의 역) |
| `shepherd/scripts/mission_rollout.py` | `scripted_role_actions` 추출 + `run_episode(scripted_roles=...)` |
| `shepherd/m4_env.py` | `mission_eval(scripted_roles=...)` 통과 |
| `shepherd/train/mappo.py` | `freeze_limiter/finisher` — no_grad + 손실 제외 + KL 조기중단 보정 |
| `shepherd/scripts/train_mappo.py` | 훅 `_override_live` / `_observe_step` (기본 항등·no-op) |
| `shepherd/scripts/train_m4.py` | `--limiter-policy` / `--finisher-policy`, `ARMS`, summary 에 팔 기록 |
| `shepherd/scripts/roles_split.py` | 신규 — 2×2 실행기 + 귀속 집계기 |
| `tests/test_role_split.py` | 신규 — P50~P62 |
| `shepherd/notify.py` | 신규 — ntfy 훅을 한 곳으로 (`train_m3a` 의 사본 제거) |

---

## 4. 판정식 (선언 · 결과 보기 전)

1차 지표는 **바꾸지 않는다**: `SHAPING_NEEDED` 영역의 무력화율 (docs/47 §4.3).
기저는 `hold` 의 Wilson 상한 **0.0128** (n=297 에서 0/297).

```
H_lim   LS 의 Wilson 하한 > 0.0128        -> 편대 학습의 단독 순이득
H_fin   SL 의 Wilson 하한 > 0.0128        -> 발사 학습의 단독 순이득
H_syn   LL 의 하한 > max(LS 상한, SL 상한) -> 결합해야만 얻어지는 상승작용
```

**풀링 규율**: 1차 판정은 시드 풀링 Wilson 으로 한다(검정력). 풀링은 시드
분산을 0 으로 가정하므로, **시드 과반이 개별로도 기저를 넘을 때만** '강함' 으로
적는다 (5시드 → 3, 3시드 → 2). 아니면 '풀링 의존' 으로 적는다. 과반 기준은
시드 수에서 자동으로 나오게 배선했다 — 하드코딩하면 시드를 늘렸을 때 기준이
조용히 느슨해진다. 집계기가 자동으로 붙인다.

**귀속 차분**은 점추정으로만 보고하고 유의성을 주장하지 않는다:

```
limiter_only  = LS - SS
finisher_only = SL - SS
synergy       = LL - LS - SL + SS
```

**2차(보고 전용, 판정 아님)**: 전체 무력화율 · 비손실 비율 · `BAND_AIM`.
docs/47 §4.4 의 P45b 가 그대로 적용된다 — `BAND_AIM` 이 아무리 좋아도 1차
판정은 바뀌지 않는다.

**null case 도 결과다**: 세 팔 모두 기저를 못 넘으면 "어느 역할을 학습시켜도
무개입+해석적 발사를 못 넘는다" 가 결과이고, 그때 결손은 특정 역할이 아니라
학습 문제 자체에 있다 (신청서 §4.7 폴백 (v)).

### 4.1 통과에 필요한 최소 성공 수

기저 상한 0.0128 기준 (최종평가 n=300, 그중 `SHAPING` 약 183):

| 표본 | 최소 k | 비율 |
|---|---:|---:|
| 시드 1개 (n=183) | 6 | 3.28 % |
| 풀링 3시드 (n=549) | 13 | 2.37 % |
| **풀링 5시드 (n=915)** | **19** | **2.08 %** |

즉 **팔당 다섯 시드 합쳐 19판**을 잡아야 통과한다. 파일럿은 0/508 이었다.

---

## 5. 무엇을 답하지 못하는가

- **한계 기여가 아니다.** §2.1 대로 각 팔은 자기 조건에서 학습하므로 차분은
  "그 역할만 학습시켰을 때의 도달점" 이다. `synergy` 항이 이 비가법성을 담는다.
- **스크립트 발사 규칙은 특권적이다.** `clean_net_threshold_crossed` 는 env 가
  계산한 `v_shot` 임계 교차다. LS 는 그 신호를 공짜로 받고 SL/LL 은 관측에서
  추론해야 한다. 즉 LS 가 이기더라도 "편대 학습이 낫다" 가 아니라 **"발사 신호를
  주면 편대 학습이 통한다"** 까지만 말할 수 있다. 이건 결과 해석에 반드시 적는다.
- **3시드는 시드 분산을 재기에 적다.** 풀링 규율(§4)이 그 한계를 표기할 뿐
  없애지는 못한다.
- **SHAPING 에서 네트 포획은 정의상 불가능하다** (docs/47 §4.4). 그 칸에서
  얻을 수 있는 것은 하드킬뿐이고, 그래서 `BAND_AIM` 을 항상 같이 본다.

---

## 6. 실행

```bash
# 0) 기저선 (이미 있으면 건너뛴다)
python -m shepherd.scripts.sweep_m4 --baseline 500          # results/hold_baseline.json
python -m shepherd.scripts.sweep_m4 --reference 300         # results/intercept_baseline.json

# 1) 명령 9개 확인
python -m shepherd.scripts.roles_split --dry-run

# 2) 서버에서 실행 (15런은 서로 독립. OMP_NUM_THREADS=1 필수)
export NTFY_TOPIC=<토픽>          # 선택 -- START·런 완료·실패·판정 요약 푸시
python -m shepherd.scripts.roles_split --run --jobs 10

# 3) 집계 (판정식이 출력에 함께 실린다)
python -m shepherd.scripts.roles_split --aggregate results/m4_roles
```

비용 (파일럿 실측 기준: 1런 4~5시간, 단일 스레드):

```
15 런 x 4.5시간 = 68 코어시간
  --jobs 10   ->  10런 동시 -> 4.5h, 남은 5런 -> 4.5h   =  약 9시간
  --jobs 15   ->  한 배치                               =  약 4.5시간
```

서버 물리코어는 24 이지만 **10 만 쓴다** (공용 기계). `--resume` 이 살아 있으므로
죽은 런은 이어서 돌릴 수 있다 — 9시간짜리 작업의 단일 실패점을 없애는 장치다.

---

## 7. 배선 스모크 (2048 스텝 — **결과가 아니다**)

배선이 끝까지 돌아가는지만 본 것이다. 학습이라 부를 수 없는 스텝수(선언값의
0.4 %)이고 최종평가도 n=40 이다. **인용 금지.**

| 팔 | 전체 무력화 | EASY 네트 포획 | BAND_AIM 무력화 | SHAPING |
|---|---:|---:|---:|---:|
| LL | 0.025 | 0.083 | 0.000 | 0.000 |
| **LS** | 0.275 | **0.833** | 0.143 | 0.000 |
| SL | 0.025 | 0.083 | 0.000 | 0.000 |
| (참고) SS 기저선 n=1500 | 0.182 | 0.832 | 0.010 | 0.000 |

여기서 **읽어도 되는 것은 하나뿐**이다: LS 의 EASY 네트 포획 0.833 이 기저선의
0.832 와 일치한다. limiter 가 사실상 안 배운 상태이므로 이 팔은 이 시점에
`hold` 와 같아야 하고, 실제로 같다 — **스크립트 발사 규칙이 기준선의 것과 동일
경로로 걸렸다는 증거**다 (§3.1 의 한 곳 원칙이 실제로 지켜졌다).

읽으면 안 되는 것: LL·SL 의 EASY 0.083. 학습된 발사 헤드가 초기화 직후이므로
낮은 것이 당연하다. 이것을 "발사 학습이 문제다"의 증거로 쓰면 안 된다 —
그 질문에 답하는 것은 500k 스텝 본런이다.

`commit nan` 이 SL 에서 나오는 것도 정상이다: limiter 가 동결돼 있어
`limiter/commit_rate` 대신 `..._proposed` 로 나간다 (§3.3).

---

## 8. 결과 (2026-08-05, 15런 완주)

### 8.1 1차 판정 — 선언된 null_case

| 팔 | SHAPING k/n | Wilson 하한 | 판정 |
|---|---:|---:|---|
| SS (hold, n=500) | 0/297 | — (상한 0.0128) | 기준 |
| LL | 0/861 | 0.0000 | 미달 |
| LS | 0/861 | 0.0000 | 미달 |
| SL | 0/861 | 0.0000 | 미달 |

`H_lim` · `H_fin` · `H_syn` 전부 미통과. 통과선은 n=861 기준 18판이었다.
**"신호 없음"이 아니라 상한이 걸린 음성 결과다** — 팔당 Wilson 상한 0.44 %,
세 팔 합(0/2583) 0.15 %.

### 8.2 ★ 설계 결함 — `H_fin` 은 검정 불가능한 가설이었다

`SL` 은 limiter 가 `hold` 이므로 커밋 비트가 항상 0 이고, 하드킬이 **구조적으로**
불가능하다. SHAPING 의 네트 포획은 4기가 거의 완벽히 배치돼야만 가능하므로
(잔여 feasible 1~46/2504) 배치를 안 하는 팔에는 그 경로도 없다. 즉 SL 은 1차
지표에서 0 아닌 값이 나올 수 없었다. §4 를 쓸 때 이것을 보지 못했다.

**1차 판정식은 바꾸지 않는다** (결과를 본 뒤 규칙을 고치는 것이 된다). 대신
한계로 기록한다: §8.1 의 "어느 역할도 못 넘었다" 는 **LL·LS 에 대해서만**
성립하고, 발사 학습의 기여는 이 지표로 측정된 적이 없다. `H_fin` 을 실제로
검정하려면 지표가 아니라 **팔**을 바꿔야 한다 (예: `intercept` 편대 + 학습 발사).
그건 새 사전등록이다.

### 8.3 2차 지표 — 결손이 발사 역할로 좁혀졌다 (보고 전용, 판정 아님)

| 팔 | 전체 무력화 | FREE (시드0) | 비손실 | BAND_AIM |
|---|---:|---:|---:|---:|
| SS (hold) | 0.182 | 0.448 | 1.00 (구조적) | 0.010 |
| **LS** | **0.187** | 0.441 | 1.00 | 0.003 |
| **SL** | **0.137** | — | 1.00 | 0.020 |
| **LL** | **0.072** | 0.228 | **0.80** | 0.000 |
| (참조) intercept | 0.240 | — | — | 0.172 |

```
LS - SS = +0.005      편대 학습은 hold 와 구분되지 않는다
SL - SS = -0.045      발사 학습은 편대를 고정해도 해석적 규칙보다 나쁘다
LL - SS = -0.110
가법 예측 LL = 0.141  실제 0.072   ->  상호작용 LL-LS-SL+SS = -0.069
```

`LL` 만 비손실 0.80 인 것이 기전을 보여준다. 300판 중 약 21판 무력화 중 4판이
하드킬인데 SHAPING 은 0 이므로, 그 하드킬은 전부 **네트로 잡을 수 있었던 구간**
에서 나왔다. 발사가 망가지자 정책이 하드킬을 대체 경로로 쓴 것이고, 그건 이
논문의 스파인(비손실 격추)을 정면으로 깬다. `LS` 의 비손실이 1.00 인 것과
대조된다 — 같은 limiter 행동공간인데 발사가 멀쩡하면 하드킬을 안 쓴다.

BAND_AIM 은 SL 이 hold 의 2배지만 **유의하지 않다** (n≈315 에서 Wilson
[0.009, 0.041] vs hold [0.003, 0.030], 크게 겹침). intercept 의 0.172 와는
자릿수가 다르다.

---

## 9. 다음

사전등록한 네 분기 중 어느 것도 그대로 발동하지 않았다 (`H_fin` 이 검정
불가였으므로). 2차가 가리키는 방향은 **발사**이고, 그건 결론이 아니라 다음
실험의 가설이다.

```
[x] 역할 동결 배선 (학습·평가 양쪽) + 한 곳 원칙
[x] 동결 액터를 손실·KL 조기중단에서 제외
[x] P50~P60 (22건). 481 passed, 4 skipped
[x] 2x2 실행기·집계기 + 판정식을 출력에 싣기
[x] 배선 스모크 3팔
[x] 서버에서 15 런 (500k, 시드 5) -- 실패 런 0
[x] 집계 -> 1차 판정 null_case (§8.1), 2차가 발사를 가리킨다 (§8.3)
[x] 발사 진단 [A] -- 관측 상한: **표현의 한계가 아니다** (§10)
[ ] 발사 진단 [B] -- 학습된 발사 헤드의 확률을 뜯어본다 (서버, 체크포인트 필요)
    `python -m shepherd.scripts.fire_audit --policy results/m4_roles/SL_s0/seed0`
[ ] 편대 알고리즘 개선(PBRS 등)은 **뒤로 미룬다** -- LS 가 hold 와 구분되지
    않는다는 것은 그 축에 아직 잴 신호가 없다는 뜻이고, 발사가 망가진 채로
    편대를 손보면 또 해석 불능에 걸린다
[ ] `H_fin` 재설계: `intercept` 편대 + 학습 발사 팔 (새 사전등록 필요)
```


---

## 10. 발사 진단 [A] — 관측 상한 (2026-08-05, 체크포인트 불필요)

§8.3 이 발사를 가리켰지만, 그것만으로는 두 가지가 안 갈린다:

```
(A) 배울 수 없다   관측에 판단 근거가 없다  -> 다음 작업은 관측 설계
(B) 못 배웠다      근거는 있는데 최적화가 못 찾았다 -> 다음 작업은 발사 헤드
```

(A) 는 **학습 없이** 잴 수 있다 — 지도학습으로 상한을 재면 된다.
`shepherd/scripts/fire_audit.py --probe` 가 그것이다. `hold` 편대(= `SL` 팔과
같은 편대) · `fire_mode="never"` 로 굴려 궤적 전체에 라벨을 붙이고,
**에피소드 단위**로 나눠 관측만으로 `clean_net_threshold_crossed` 를 맞힌다.

### 10.1 결과 (140 에피소드 · 3894 스텝 · 교차 122 = 3.13 %)

| 예측기 | 테스트 AUC | 비고 |
|---|---:|---|
| 양성 대조 (`v_shot_soft` 실측) | **1.000** | 이게 1.0 이 아니면 배선이 틀린 것 |
| **관측 차원 하나** (`obs[60]`) | **1.000** | 관측이 `v_shot_soft` 를 그대로 싣고 있다 |
| 관측 전체 · 로지스틱 | 0.9997 | |
| 관측 전체 · MLP(128) | 0.9989 | |

그리고 **단일 임계 하나로 라벨이 정확히 재현된다** (홀드아웃 정확도 · 재현율 ·
정밀도 전부 1.000):

```
clean_net_threshold_crossed  ==  obs[60] >= theta_fire (0.9)
    (이 운용점에서 boxed_in 은 교차 123 스텝 중 0 건 -- 비용 0)
```

### 10.2 읽는 법

**(A) 는 죽었다.** 발사 판단은 정책이 이미 받고 있는 관측 벡터의 **차원 하나에
임계 하나**다. 표현의 한계가 아니고, 관측 설계를 손댈 이유가 없다. `docs/48 §5`
에 "스크립트 발사 규칙은 특권적이다" 라고 적어 둔 한계는 **틀렸다** — 그 규칙이
쓰는 신호는 관측에 그대로 있다. 정정한다.

따라서 결손은 **(B) 최적화**다. 그리고 이건 파일럿의 확률 붕괴 관측
(p=0.0002 / p=0.9999) 과 같은 방향을 가리킨다.

부수로 눈에 띈 것: 관측 전체를 준 모델이 **한 차원만 준 것보다 약간 나쁘다**
(0.9989 vs 1.000). 64 개의 무관한 차원이 섞이면 지도학습조차 살짝 흐려진다는
뜻이고, 보상만으로 배워야 하는 RL 에는 그보다 훨씬 불리하게 작용한다.

### 10.3 남은 것 — 진단 [B]

체크포인트가 필요하므로 서버에서 돈다.

```bash
python -m shepherd.scripts.fire_audit --policy results/m4_roles/SL_s0/seed0
```

보는 것: `p_fire` 분포(0/1 로 굳었는가) · 게이트 조건부 `E[p|교차]` vs
`E[p|비교차]` · `AUC(p_fire, 교차)`. AUC ≈ 0.5 이고 조건부 두 값이 같으면
정책은 **게이트를 안 보고 쏜다** — 관측에 답이 있는데도.

P63 이 이 진단을 지킨다: AUC 경계값(동점 → 0.5), **관측 인덱스가 실제
`v_shot_soft` 채널인지**(배치가 바뀌면 결론이 뒤집힌다), 양성 대조 1.0,
수집기 라벨이 env 술어와 일치. 정렬은 *스텝 직전 관측 ↔ 그 스텝 info* 이고
음성 대조(이동 후 관측 짝은 상관 0.90)로 그 짝만 맞다는 것까지 확인한다.
