# 핸드오프 — 2026-08-06

새 Claude Code 세션이 이 리포에서 이어서 작업할 때 **먼저 읽는 문서.**
자기완결적으로 썼다. 리포는 `~/newURP` (서버) / `C:\Users\Teemo\Desktop\ANDES\URP\newURP` (로컬).

---

## 1. 이 프로젝트가 뭔가

last-mile 대드론 협력 방어. 회랑을 침투하는 반응형 스크립트 공격자 1 기를
상대로 **limiter 4 기**(셰이핑 기동으로 도달가능집합 압축)와 **finisher 1 기**
(그물 발사, 탄 K=1, **위치 고정**)가 협력한다. 성공은 **비손실 포획**이고
그게 논문의 스파인이다.

원 연구 질문 (불변): *"이종 다중 드론이 MARL 을 통해 공격 드론을 협력적으로
양치기·성형하여 포획 가능한 상태로 유도할 수 있는가."*

**비준된 아키텍처는 하이브리드**다 (2026-07-19, `docs/09:1032`):
MARL 협력 성형 + **규칙 기반 자율 종말 가드**. `learned-fire 계열은 중단`
됐고, 그 실패는 하이브리드 선택의 근거 증거로 논문에 들어간다.

---

## 2. 지금까지 확립된 것 (수치는 전부 재현 가능)

```
학습이 손튜닝 기준선을 못 넘는다
    SS(무개입+규칙발사) 0.182 / LS(학습편대+규칙발사) 0.187
    SL(학습발사) 0.137 / LL(둘 다 학습) 0.072          docs/48

원인은 finisher 의 **조준**
    게이트 개방률 해석적 0.0430 vs 학습 0.0262 (0.61x)  docs/48 §12
    행동복제로 한 방에 97 % 회복. RL 이 지운다.
    상시 BC 항(λ=0.1)으로 격차의 24.5 % 유지            docs/49 §8

★ 매개가 붙었다 — 게이트와 성적이 같이 움직인다
    BCa 게이트 +24.5 % / 성적 +20.6 %
    BCw 게이트 −8.6 %  / 성적 −10.3 %   시드 부호일치 4/5   docs/49 §8.3

편대 학습은 측정 가능한 기여가 없다 (LS ≈ SS)
포획기를 움직여도 안 바뀐다 (구제 0/297, 슬루 제한과도 무관)  docs/51
```

---

## 3. ★ 2026-08-06 에 뒤집힌 것들 — **이걸 모르면 옛 결론을 인용하게 된다**

| 철회/정정 | 어디 |
|---|---|
| "LL 이 하드킬로 도피한다" → **하드킬은 0.** `nondestructive_frac` 의 0/0 퇴화값 오독 | docs/48 정정 9, docs/50 §1.3 |
| "성형 채널이 구조적으로 무력하다" → **반증.** SHAPING 111/122 에서 접촉 도달 가능 | docs/52 §1 |
| "성공 = 모든 탈출구 봉쇄" → **선택적 압축**이다. 전부 막으면 `boxed_in` = 실패 | docs/52 §1.2, §3.5 |
| "이동이 나쁜 건 슬루 부담 때문" → **기각.** 무한 슬루로 500/500 동일 | docs/51 §9 |
| 2a 를 Block 1 에서 돌린 것 → union prefix 인 **쉬운 부분집합**. 재실행함 | docs/52 §4.2, §4.4 |

---

## 4. 현재 위치 — 오라클 계층

```
1.   단일 접촉 도달성        감사완료  SHAPING 111/122        docs/52 §4
2a-E 선택적 coverage 필요조건 감사완료  SHAPING 19/23          docs/52 §4.4
2b   4 기 동시 coverage       witness 8/19, **전부 knife-edge** docs/52 §8.1d
       m_sel 최대 2.3 cm · 중앙 1.4 cm
2b-t 시각별 재최적화 (ep18)   흩어진 3 tick, continuation 끊김  docs/52 §8.1e
       인접 독립해 이동 최대 50.9 m/s (v_max 25.9)
```

**핵심 문장** (이 이상으로 쓰지 말 것):

> 현행 union evaluator 에서 선택적 snapshot witness 가 최소 8 개 에피소드에서
> 확인됐다. 발견된 모든 witness 의 결합 여유는 2.3 cm 이하였고, 한 사례를
> 시각별로 풀어 보니 clean snapshot 이 여러 tick 에 흩어져 존재하나 인접 해들이
> 미터 단위로 떨어져 있어 동역학적 연결이 확인되지 않았다. **solver 가 시드에
> 민감함이 실측돼, 보고된 support 는 전부 하한이다.**

---

## 5. ★ 다음 작업 = `docs/54` 계약 개정

세 비정합이 코드로 확인됐다 (docs/53):

```
1. 접촉 event resolver **부재**  env_sys.py:239 -- 하드킬은 커밋 비트로만 발동.
                                 공격자가 kill_radius 를 통과해도 아무 일 없음
2. net miss **즉시 종료**        env.py:356 -- 비준된 "2모드+1핸드오프" 와 불일치
3. v_soft 는 확률이 아니다       union 블록별 밀도 상이·확률질량 가중 없음
```

3-arm 실측으로 **우선순위가 뒤집혔다** (docs/53 §4.5b):

```
A  현행·무발사       접촉 0.765  침투 1.000
C1 종료만 억제       침투 0.941        <- 거의 안 바뀐다
C2 + 접촉 resolver   침투 0.235  무력화 0.706
```

**즉 접촉 resolver(R1)가 handoff(R2)보다 먼저다.**

작업 순서는 `docs/54 §7`:

```
[ ] R1 배선 + P78~P80        (기본값 off = 비트 동일이 P78)
[ ] V2 검증 — C2 수치 재현 (무력화 ~0.706 / 침투 ~0.235)
[ ] R2 배선 + P81~P82
[ ] R3·R4 지표 분리 · frontier 집계
[ ] oracle / scripted / RL 3-way  <- 이거 없이 "물리 한계" 라고 쓰지 않는다
```

---

## 6. ★ 이 리포의 작업 규율 (어기면 같은 사고가 반복된다)

`docs/50 §3` 에 병목 다섯 개가 등재돼 있다. **전부 알고리즘이 아니라 측정·해석
에서 났다.**

```
1. 무엇을 재는지 잘못 정함        hold 가 무개입이 아니었다
2. 검정 불가능한 가설             H_fin (SL 은 커밋 비트가 구조적 0)
3. 매개를 가정으로 둠             개방률 -> 성적 (해소됨)
4. 잰 값을 잘못 읽음              0/0 퇴화값을 하드킬로
5. 증거 강도 < 주장 강도          휴리스틱 실패를 물리적 불가능으로
6. 성공 술어를 반대로 이해        봉쇄 vs 압축
```

**실무 규칙:**

- **"구조적으로 / 물리적으로 불가능" 은 인증을 만든 뒤에만 쓴다.** 휴리스틱
  실패는 상한이 아니다.
- **목적함수를 짜기 전에 판정 함수의 코드를 먼저 읽는다.**
- 집계 평균에서 원인을 역산할 때는 **분모가 0 이 될 수 있는지부터** 본다.
- 오라클 결과를 환경 결과로 부르기 전에 **동치 테스트**를 통과시킨다
  (`tests/test_union_trace.py` P74~P77 이 그 계약).
- 사전등록을 **결과 보기 전에** 쓰고, 결과를 본 뒤 판정식을 바꾸지 않는다.
  바꿔야 하면 **새 질문의 새 사전등록**으로 만든다.
- 새 실험 전에 **`docs/09` 비준 기록을 확인한다.** 2026-08-06 에 종료된
  라인(fire-head 튜닝)을 모르고 되살린 사고가 있었다.

---

## 7. 코드 지도

```
shepherd/game/viability.py          v_shot · union · UnionTrace(return_paths)
shepherd/env.py                     핵심 env. :332 finisher 병진, :356 종료
shepherd/env_sys.py                 M4 래퍼. :239 커밋 비트 -> 하드킬
shepherd/scripts/
  mission_rollout.py                스크립트 역할 (한 곳 원칙). LIMITER_MODES
  contact_reachability.py           단계 1
  coverage_oracle.py                단계 2a (union 기반)
  selective_snapshot_oracle.py      단계 2b witness search
  witness_margin_audit.py           2b 여유 감사
  temporal_support.py               시각별 재최적화
  boxed_arm_audit.py                boxed 3-arm
  roles_split.py                    역할 분리 실행기
  train_m4.py                       M4 학습 (--aim-bc none/warm/aux)
tests/test_union_trace.py           P74~P77 oracle-env 동치 계약 ★
```

전체 스위트: **520 passed / 4 skipped** 근방 (서버 기준 +2).

---

## 8. 열려 있는 것

```
[ ] docs/54 계약 개정 (§5)
[ ] 2b multi-seed — support 폭이 전부 하한이라 결론 전 필수
[ ] A/B 표본 민감도 — 강건 ep18 / 중앙 ep15 / 취약 ep17
[ ] docs/49 §7 의 finisher/bc_cosine_loss 가 **저장 안 됨** (사후 복구는 가능)
[ ] 편대(협력 성형) 질문 — 논문 중심인데 아직 제대로 물어진 적 없음 (docs/50 §2)
```

**마감일이 (A) 편대 재개 vs (B) 현 자산으로 집필 을 가른다** (docs/50 §5).
그 정보가 없어서 결정이 미뤄져 있다.
