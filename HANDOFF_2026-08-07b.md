# 세션 핸드오프 2026-08-07b — 위협 계약 v3 확립·동결, 다음 = P92/93/95 + docs/63

너는 `newURP` 를 이어서 작업하는 **검증 우선 연구 엔지니어**다. 이 문서는
2026-08-07 저녁 세션(위협 v3 설계→4차 비준→배선→게이트 전판→V6/V7→리뷰 5→
docs/61 동결→P94 GREEN)의 승계본이다. 이전 핸드오프(`HANDOFF_2026-08-07.md`,
`HANDOFF_2026-08-06.md`)의 규율은 전부 유효하며 아래가 누적된다.

---

## 0. 저장소·브랜치·기록처

- 브랜치 `feat/scale-up-v2` (HEAD f4fa6bf+, push 됨). 서버 = 사용자가 직접
  실행 (server4/5/6, hjhong — 세션에서 ~/newURP 미확인이었으나 사용자가
  V6 를 서버에서 완주함. 서버 런은 명령 set 만 만들어 넘기면 됨).
- ★ **Notion Daily Log 임시 중단** (요금제 — insert 가 성공 응답을 줘도
  실제 미반영). 일자별 기록 = `temp_research_note/YYYY-MM-DD_*.md`
  (판정형 제목·커밋 해시·다음 단계). memory `notion-daily-log` 갱신됨.
- 시작 절차: `git log -8 --oneline` · `pytest -q --ignore=tests/test_fire_audit.py`
  (로컬 60 skip 정상. **P87 golden 은 win32 한정 bit-계약** — 서버는 atol 1e-9).

## 1. 읽기 순서

```
이 핸드오프
temp_research_note/2026-08-07_threat_v3.md   ★ 이번 세션 전체 서사 (12항목)
docs/61_train_distribution_prereg.md         ★ r2 = 비준·동결 (불변!)
docs/62_review5_verdicts.md                  리뷰 5 판정 + 명칭 규율
docs/60_threat_v3_design.md                  v3 계약 r4 + 게이트 결과 §5.1~5.3
```

## 2. ★ 확립된 사다리 (되돌리지 말 것)

```
A2         defender geometry -> attacker action 채널 없음 (P84 artifact)
v3 + P88   방향성 있게 조작 가능 (directionally controllable causal channel)
P94 GREEN  자연 궤적에서 mission-relevant (발산>=1m 46/50 · label 변화 7/50)
           = "naturally expressed, mission-relevant shepherding causal channel"
           ★ "learnable" 은 보류 -- MARL 결과 후에만 "learned shepherding"
다음: P95(strata 실효) -> docs/63(baseline 동결) -> MARL ->
      static/active mechanism audit -> IID/CPA-OOD/A4 반증
```

핵심 구분 (docs/61 §5.1): P94 의 label 변화는 **공격자 reactive competence**
증거이지 defender shepherding 성과가 아니다. MARL 성능이 좋아도
static/active 대조 없이 "learned shepherding" 으로 점프하지 않는다.

## 3. 다음 세션의 작업 — 두 갈래, ★서로 독립 커밋 사슬 (Hyunjun 지시)

```
갈래 1: P92 -> P93 -> P95  (docs/61 §5 판정식 그대로)
  P92  draw_threat_v3 배선 (m4_config.draw_threat 패턴, SHA-256, 3×3 셀
       균등 + 셀 내 jitter, layer train/iid) + 결정론·coverage·경계·NESTING
  P93  TRAIN draw 50판 무방어 -> 50/50 침투·절단 0 (episode_len_train=1100 확정)
  P95  paired CRN reactivity audit: base 30판 고정, reaction stratum 만
       weak/med/strong 교체. primary = R_route (활성 스텝 평균 realized
       route 크기). 평균·중앙값 둘 다 weak<med<strong.
       ★ 실패 = MARL 실패가 아니라 TRAIN taxonomy 실패 -- 성능 안 본
       상태에서 셀 경계 재사전등록. 기준 완화로 PASS 금지.

갈래 2: docs/63 scripted baseline 동결 (설계 문서 -> Hyunjun 비준)
  정할 것 = 성능 숫자가 아니라: 관측 집합 · bearing 사용 방식 · limiter
  역할 배분 · rule family 허용 범위 · 튜닝 파라미터 목록 · 총 튜닝 budget ·
  baseline 설계가 볼 수 있는 데이터 범위.
  ★ IID/OOD 결과를 baseline 튜닝에 절대 사용 금지까지 문서에 잠근다.
```

**새 아이디어 추가 금지** — 두 blocker 닫고 MARL 질문 복귀가 유일 목표.

## 4. 이번 세션 신규 오류/교훈 (기존 1~22 에 추가)

```
23. 임의값을 임의값에 앵커하지 않는다 (route_probe=ring_radius 순환 -> 기각.
    해법 = 파라미터 소멸(angular-gap) 또는 기존 선언량 합성(r_block))
24. golden bit-계약은 생성 플랫폼 한정 (libm ULP) -- 타 플랫폼은 atol 대조
25. 대칭 gate 는 퇴화 상태(공면 동률)를 노린다 -- tie-break 는 반드시
    공변으로 설계. 교정-재선언 허용 경계 5조건 = docs/62 §3
26. "요청" 수치와 "실현 기전"을 구분 (route z-지배 0.81 vs 육안 관성+jink --
    viewer 가 수치 해석을 이긴 세 번째 사례. viz-first)
27. 지표 정의는 nesting 을 존중 (sprint_steps 를 v_ref>speed 로 재다가
    sprint 없는 arm 에서 오검출 -- v_ref vs v_nominal 로 정정)
28. 분포 명칭: balanced experimental design distribution (빈도 추정 아님) ·
    OOD = 반례 장치 (인증서 아님) · comparator 는 결과 전 고정
```

## 5. 도구 인벤토리 (신규분)

```
shepherd/scale_v2.py                THREAT_V3_NOMINAL·THREAT_V3_SPAWN·
                                    THREAT_V3_STANDBY·V3_ARMS (C/CS/FULL)
shepherd/scripts/threat_v3_gates.py P88/P89/P90/P91a/P91b/P94 (전부 green,
                                    results/threat_v3_*.json)
shepherd/scripts/scale_v3_baseline.py  V6 러너 (--arm --eps, 계측 diag)
shepherd/scripts/dump_trajectory.py    --v3 지원
viz/trajectory_viewer_template.html    fit-to-screen + 속도 색상
tests/test_threat_v3.py (19) · tests/golden/p87_prewiring.json (win32 bit)
뷰어 artifact: v3 = claude.ai/code/artifact/0a31f6b9-… (legacy 650b745b ·
v2 d0cb484f 별도 유지 -- 혼합 금지)
```

전체 스위트 504 passed / 60 skipped (로컬) 근방.

## 6. 금지·경계 (요약)

- docs/61 r2 불변. 판정식 결과 후 불변경 — 조정은 새 사전등록.
- "learnable/learned shepherding" 은 MARL+static/active 후에만.
- "TRAIN 범위가 nominal 과 독립" 금지 (V6-비의존만 주장).
- P91b 류 교정은 docs/62 §3 의 5조건 안에서만 — threat tuning 면허 아님.
- r_nk 튜닝·oracle 확장 금지 유지. legacy/v2/v3 수치 혼합 금지.
```
