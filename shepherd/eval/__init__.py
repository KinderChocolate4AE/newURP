"""shepherd.eval — measured exchange-frontier, regime-map N*(agility), figures.
Replaces the static/heuristic prototype frontier with MARL-rollout measurements
under a best-response adversary (the BAR: show the frontier MOVES, not capture-rate up).
"""
