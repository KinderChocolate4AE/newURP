# MARL LL v3 1런 = 완전 영점 (0/300, 전 구간 침투) — 사전등록 사다리대로 LS 팔 진행

**2026-08-08 · 서버 런 (smoke ALL PASS · parity green · CUBLAS 결정론 설정
후 fresh 시작). arm=LL · threat_layer=train · dist hash efeffcbf(FINAL
FREEZE 일치) · contract hash e275ca1 · w_kill=0.5 · seed 0 · resumed 0 ·
488 upd 완주. 결과 커밋 = 서버 push (summary/curve/contract_final).**

## 결과 (판정용 final eval 300판)

```
무력화 0.000 · 침투 1.000 · 비손실 0/0
counts: PENETRATED 300 / NET 0 / CWC 0 / HARD_KILL 0 / SPENT 0 / TRUNC 0
regime 분해: SHAPING_NEEDED 0/164 · FREE_CAPTURE 0/136
곡선 25회 eval 전부 무력화 0 (학습 중 단 한 번도 비영점 없음)
```

## 두-줄 기록 (docs 규율 동형 — 허용 상한)

> **LL (end-to-end 학습) 은 v3 TRAIN 분포·현 학습 계약·1 시드에서 무력화
> 0/300 — legacy 에서 실증된 발사·조준 credit 병목 (C003) 의 v3 재현과
> 정합적.**
>
> 허용 = "한 운용점(w_kill 0.5)·한 시드·현 PPO 계약에서 LL 의 순이득이
> 측정되지 않았다". 금지 = "학습 불가능" (P88/P94 는 environment
> controllability 증거 — learnability 는 계약 의존, docs/65 §4).

## 관찰 (해석 보류 — 후속 진단 후보)

- 학습 중 commit rate ~0.44~0.51 로 건강하게 탐색했는데 **HARD_KILL 0** —
  커밋 제안이 전부 GEOM_FAIL/VETO 로 해소됐거나 소모 후 조기 고갈로 추정.
  FREE_CAPTURE 구간(발사만 되면 잡히는 쉬운 136판)까지 0 인 것은 발사
  링크가 아예 안 붙었다는 신호와 정합. 확정은 서버의
  `threat_log_final.json` (n_committed/veto) + ep_records (fire_events/
  wasted) 읽기로 — **진단 읽기는 허용, 이걸 근거로 reward/계약 변경은
  금지** (docs/65 §7).
- SPENT_FAIL 0 = R2 handoff 정상 작동 (miss 가 종료 라벨로 안 샘).

## 다음 = LS 팔 (docs/48 선언 팔 — 새 비준 불요)

learned limiter + **scripted clean-fire**: 발사 병목을 MARL 팔과 scripted
baseline(c5) 양쪽에서 동일하게 제거 → **순수 편대 협력 비교**로 headline
질문 성립. LL 실패는 headline 을 막지 않는다.

```
python -m shepherd.scripts.train_m4 --threat-layer train --seed 0 \
    --finisher-policy scripted --device cuda --output results/m4_v3_train_LS
```

관전 포인트 (곡선 규율 그대로 — 판정은 final/headline 만): LS 는 발사가
공짜라 무력화가 scripted floor(~0.1 자릿수) 근처에서 시작해야 정상.
질문은 편대 학습이 그 위로 올리는가. 이후 headline = hold vs scripted(c5)
vs MARL-LS, **IID 10000..10299** paired.

금지선 재확인: reward 재설계 · 위협 완화 · LL 재시도 튜닝 · 결과 본 뒤
baseline 변경 — 전부 안 함.
