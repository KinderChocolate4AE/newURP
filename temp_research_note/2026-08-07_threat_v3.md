# 2026-08-07 (이어서) — 위협 계약 v3: 설계 → 4차 비준 → 배선 → 게이트 전판 green → V6

> **Notion Daily Log 임시 대체 노트.** 요금제 문제로 Notion 기록이 막혀
> `temp_research_note/` 에 적는다 (2026-08-07 Hyunjun 지시). Notion 마지막
> 반영 항목 = "시각화 5분이 수치 6주를 이김 … 다음 = 위협 계약 v3 (종축
> 속도·횡 반응·방위 랜덤)". **그 다음 항목부터 여기 기록.** Notion 복구 시
> 이 파일을 이관.

---

## 1. 위협 계약 v3 설계 문서 작성 (docs/60 r0, 커밋 `94b786e`) — 비준 대기

핸드오프 승계 후 유일 작업 = v3 설계 (배선·결과 없음, "설계 → 비준 → 배선"
순서 준수).

- 결함↔근거↔축 1:1 표: 등속 순항(V4-800 대응시간 미사용) → (i) 종축 속도 /
  횡 무반응(P84 42/42) → (ii) 횡 반응성 / +x 고정 스폰 → (iii) 방위 랜덤화.
- **경로 권고 = B (반응형 attacker)**: A(주장 축소)를 고르면 원 연구 질문이
  문서상 종결 — 축소 실험 없이 내릴 이유 없음. B 실패 시 A fallback 은 항상
  남는다 (역은 불성립).
- 설계 원칙: 능력은 기 선언(`adversary_v_max=1.5×att_speed`)·행동만 추가.
  (ii)는 기존 `_route_accel` 활성화 + `sense_range` 신설. (iii)은
  v3.0/v3.1 단계화.
- 게이트 선언: P87 NESTING · P88 manipulation · P89 능력 · P90 무방어 침투 ·
  V6 baseline · V7 뷰어. 지평선 산술(오류 18): episode_len 800→1000 제안.

## 2. 조건부 비준 접수 → r1 반영 (커밋 `4d87f53`)

Hyunjun 조건부 비준 6수정 전부 반영:

1. **nested arm V3-C/V3-CS/V3-FULL** (동일 seed) — "limiter 부동 비용" 해석
   철회 (동일 V3-FULL 위 static vs active limiter 비교로만 측정).
2. **P88 → 방향성 gate 5항** (mirror 부호반전·최대간극 방향 일치·in/out·
   positive control).
3. **P89 saturation audit** (jink 0.6 + route 0.5 > 1 lateral 포화 가능 —
   raw pre-clip·clip 비율·route authority 기록 의무).
4. **feint → slowdown 재명명 + 기전 주장 보류** (코드 사실: v_shot 도달집합은
   가속 유계라 스프린트 가능성이 이미 샘플 안).
5. 축 (iii) = **rotational randomization** 재명명 + P91 equivariance 검사.
6. **자유도 10개 동결표** (r0 은 route_gain·azimuth 누락).

route_probe 는 코드 확인 후 5.0 m 절대값(=ring_radius) 재선언 제안 →
배선 착수 조건 = 이 1건.

## 3. v3 구조 전환: 점 위협 → 분포 위협 (r2, 커밋 `ea77aed`)

Hyunjun 2차 지시로 프레임 전환: "공격자 설정값 잘 고르기" 금지.

- **위협 5층**: NOMINAL(P87~P91 검증 전용·학습 금지) / TRAIN(사전등록 분포,
  docs/61 로 결과 전 동결) / IID(held-out) / OOD(구조 상이) / A4(self-play =
  red-team falsifier 층, 주 학습 환경 금지).
- 평가 = mean + Q_0.1 + CVaR. headline = "hold vs MARL across threat
  distribution".
- **route_probe 파라미터 소멸**: probe-circle 16점 스캔 → angular-gap
  정식화 (blockage 반각 α=asin(r_block/|o|), r_block = 기존 repel 반경 합성
  → 신규 상수 0, 잔여 = sense_range·route_gain 2개). spread-기반 대안은
  "attacker perception 이 defender 함수화 → exploit" 사유로 기각.
- **ring-회전 정렬 폐기 → bearing-독립 4방위 symmetric standby** (R 12 m
  nominal·φ₀ 랜덤·탐지 완료 가정 명시) — threat-relative formation 은
  policy 의 몫. ring_radius 는 standby 초기조건으로 강등. COMA cf 의존
  감사 = P51 과 묶음. P91b(bearing mirror) 추가.

## 4. r3 최종 비준 → 배선 완료 (docs `55bbe1e` / code `c46cbc8`)

r3 3수정(angular-gap = instantaneous transverse heuristic 범위 제한 ·
r_block 재사용 = nominal modeling choice 명시 · P91 → P91a backend isotropy
/ P91b bearing sanity 분리) + φ₀~U[0,π/2) 반영 후 배선 승인.

배선: attacker_ladder v3 필드 5개(기본 off) + v_ref 속도 프로파일 +
_route_accel angular-gap 교체(route_probe 소멸), env_adv v_max 전달,
SpawnSpec r_range/azimuth, StandbySpec/apply_standby(COMA cf 동기화),
build_m4_env(standby), scale_v2 THREAT_V3_NOMINAL/V3_ARMS.

**P87 = 배선 전 커밋에서 생성한 golden(공격자 300점 + spawn 10 draw)과
bit-대조로 통과.** mirror 부호반전·sense 게이팅·전원주 봉쇄·속도 프로파일·
standby 대칭/결정론 단위검증 포함. 전체 스위트 **504 passed / 60 skipped**
(회귀 0).

## 5. 게이트 전판 green: causal channel 공식 개통 (`abc56db` 스크립트 결과 전 / `c2da8e9` 결과)

- **P88 7/7 ALL PASS** — mirror 부호반전·angular-gap 방향 일치·sense 안
  발산·밖 bit-동일·positive control. **"defender geometry → attacker
  action" 방향성 causal channel 최초 개통** (P84 의 42/42 무반응 공식 역전;
  유도 성공은 여전히 별도 입증 대상).
- **P90 100/100 PENETRATED·절단 0** (최장 847 스텝) → episode_len 1000 확정.
- **P91a 4/4 PASS** (dyn ~1e-13·v_shot 0.000 — evaluator 포함 hidden +x 없음).
- **P91b 1차 FAIL → 설계 목적대로 대칭 결함 사전 검출**: 공면 퇴화에서
  route tie-break 가 ±z 동률 free arc 를 리스트 순서로 분기 (t=223 z성분
  ±21.96, dev 2.42 m) → 거동 정의 교정 재선언(세계 +z 선호 + 1e-9 양자화,
  route 기여 이력 0 이라 소급 오염 없음) → 재실행 8.95e-16 PASS.
- **P89 capability PASS** (|v|/v_max ≤ 1.0000), 단 saturation 실질적:
  sprint 구간 clip 36% 평균(최대 76%)·route authority 0.53 — 축 (i)(ii)
  sprint 구간 부분 경합. "광범위" 임계 미사전등록 명시 → Hyunjun 판정 대기.

## 6. P89 재비준(r4) + V6 nominal nested baseline (`e8b3ebd` r4 / `33165ed` 결과 / `7c205b1` 기록)

**P89 재비준**: route_gain 0.5 **확정** — 단일 3D 가속도 budget 공유를
의도된 계약으로 명문화(docs/60 §2.2 영문 계약문), 축 (i)(ii) independence
주장 기각, 수치 보고 후 인하 = 사후 튜닝 금지. +z tie-break 는 TRAIN
랜덤화 금지·OOD −z variant 예약. 순서 개정: V6/V7 → docs/61 (TRAIN 범위는
V6 결과 비의존).

**V6 (서버 12샤드, 3 arm × n=100, hold/clean)** — sanity (a)~(f) 전 항목
이상 없음:

| | V3-C | V3-CS | V3-FULL |
|---|---|---|---|
| 라벨 (침투/포획) | 85/15 | 87/13 | 87/13 |
| route 활성 (스텝 비율) | 0.106 | 0.167 | 0.115 |
| route z-지배 비율 | 0.55 | 0.51 | **0.81** |
| z_max 평균 (최대) | 6.4 (17.3) | 9.1 (22.6) | 4.0 (8.5) |
| sprint 구간 clip | 0.06 | 0.25 | 0.39 |
| sprint/slowdown 개입 판 | 0/100 | 100/100 | 100/100 |
| redeploy_ratio | (0.52 off-label) | — | **0.115** |

- **vertical escape 실재** — FULL 에서 route 회피의 81%가 z-지배 (수평
  standby 의 평면 봉쇄 → 위 탈출 지배). MARL 3D 협력의 시험대 (r4 판단 정합).
- nesting 정확 (속도 프로파일 C 0/100 · CS/FULL 100/100).
- FULL 재배치 기하 여유 (redeploy_ratio 0.115) — "탐지 후 재배치" 문제 성립.
- self-defeat 없음 (|v|/v_max=1.0000 · fire 18~24/100 · min_d 1.0~1.4 m).

사이드: P87 golden 을 생성 플랫폼(win32) 한정 bit-계약으로 정정 (서버
libm ULP, `5aa567f`).

## 7. V7 뷰어 게시 (커밋 `949ba7a`)

- `dump_trajectory --v3` 추가 (V3-FULL NOMINAL: standby 4방위 + 방위 스폰).
- 후보 5판 덤프: **ep1 PENETRATED** (z_max 8.47·route 0.42 — vertical
  escape 대표판) · **ep4/23/53/56 CAPTURED** (23·4 = NET_CAPTURE 대조,
  53·56 = sprint 구간 clip 100% 판).
- **artifact (v3 전용, v2 뷰어와 분리 게시 — legacy/v2/v3 혼합 금지 규율)**:
  `claude.ai/code/artifact/0a31f6b9-08bb-4d58-8776-9f2f01d87057`
  (legacy = …/650b745b · v2 = …/d0cb484f 유지)

**Hyunjun 육안 검사 항목**: ① ep1 에서 "평면 봉쇄 → over-the-top escape"
실존 ② standby 4방위·방위 스폰이 의도대로 ③ 저속(60–90 m)→스프린트(≤60 m)
전환 육안 식별 ④ angular-gap 회피 기동의 자연스러움.

## 8. V7 육안 판독 접수 + 뷰어 개선 (docs/60 §5.3, 커밋 `8ec8710`)

- **★ 해석 정정 (①)**: ep1 vertical move = net 회피 후 **관성**으로 뒤로
  밀리며 위로. 실제 회피 = jink 아다리. → V6 (b) "z-지배 0.81" 은 route 의
  **요청** 수치이지 실현 기전의 증거 아님. route 인과 귀속 = paired
  ablation (동일 seed·route on/off) 필요 시 별도 사전등록. viz-first 재확인.
- ②③ 승인 · ④ nominal 에서 미세한 것이 설계와 일치 (blockage 반각
  asin(0.75/d) ≈ 수 도).
- 뷰어 개선 재게시 (동일 artifact 0a31f6b9): fit-to-screen + 속도 색상
  (파랑 느림→주황 빠름) + 범례.
- 확인 질답: 공격자 = A2-v3-nominal (전진 P-drive + jink 0.6/1.5 Hz +
  homing 4.0 + repel 1.125 m + route 0.5/sense 30 + sprint/slowdown +
  종말 3 m 게이트). "미세 요동·직진 인상" 은 설계된 nominal — jink 변위
  진폭 0.07~0.53 m·homing 감쇠·종말 게이트. 반응성 크기는 TRAIN 분포 축.
  수비자 = **hold 기저선 (부동) 맞음** — limiter 무개입·finisher 위치 고정
  (조준 slew 만). limiter 기동은 docs/61 뒤 scripted/MARL 이 채울 자리.

## 9. docs/61 초안 + 리뷰 프롬프트 5 작성

- **docs/61 r0** (`61_train_distribution_prereg.md`): 3×3 stratification
  (반응성 weak/med/strong × 속도 cruise/sprint/sprint+slowdown, 9셀 균등 +
  셀 내 jitter) · 능력 브래킷과 곱 · R_standby U[8,16] · **episode_len_train
  = 1100** (TRAIN 최악 모서리 재산술: 43.75+7.5+1.25 = 52.5 s — nominal 의
  1000 과 별도) · IID namespace 분리 · OOD 4종 (−z tie-break/CPA/timing/
  corner) · P92(분포 배선)·P93(TRAIN 침투 보존 50판) 게이트 · 평가 = E[J] ∧
  최악 셀 비열화 동시 조건 + CVaR_0.1 limiter 손실 · 비준 체크리스트 10항.
- **리뷰 프롬프트 5** (`review_prompt_threat_v3.md`, 자기완결·적대적):
  "전부 green 인 하루는 그 자체로 의심스럽다" — 공격 대상 주장 7개
  (P88 channel 존재 vs 학습 가능 신호 · P91b 교정-재선언 경계 · P89 결함의
  계약화 · V6 라벨 불변의 이중 해석 · angular-gap 종축 소거 · 분포 설계
  4결함 후보 · 미실시 목록 위험도 순위) + 최위험 가정 지정 + 동결 전 수정
  최소 목록 (3개 이하) 요청.

## 10. 리뷰 5 판정 접수 → docs/61 r1 + ★P94 GREEN

- **리뷰 5 판정** (docs/62): 주장 1~5 조건부 유지 · 6(분포 최소가정)·7(미실시
  이월) **기각**. 총평 = "P87/89/90/91 은 engineering gates, 과학적 gate 는
  P88 뿐이고 그것도 존재까지만". 최위험 가정 = **자연 상태에서 channel 이
  충분한 빈도·크기로 작동하는가**. 동결 전 수정 3개 지정.
- **docs/61 r1** (`fb4fcbd`): ① P94 natural-state route paired ablation
  게이트 신설 ② "balanced experimental design distribution" 재명명 +
  nominal-중심 인정 + P95 realized-reactivity audit ③ lexicographic
  primary endpoint (NET_CAPTURE 개선 → total defense 비열화 → 최악 셀
  비열화, margin 5 %p + 95% CI) + scripted baseline 동결 문서 docs/63 예약.
  부수: A2 상속값 = fixed inherited nuisance · OOD = 반례 장치 (인증서
  아님) · 수평 섹터 scope limitation 명시.
- **★ P94 실행 (게이트 커밋 `f5c75b6` 결과 전 → 결과 `6dccf93`): GREEN** —
  paired 50판 (route ON vs OFF, 자연 상태 hold/clean):
  발산 ≥1 m **46/50** (기준 25/50) · 발산 >0 50/50 · **label 변화 7/50**
  (HARD_KILL→침투 4 · 포획→침투 2 · HARD_KILL→포획 1 — route 가 방어측
  무력화를 회피하는 방향) · fire 변화 5/50 · route 활성률 0.13.
  → 리뷰 5 최위험 가정이 이 표본에서 지지. "학습 가능한 shepherding
  channel" 명칭 제한 해제 (학습이 실제 이용했는지는 MARL+static/active 몫).

**다음 = ① Hyunjun docs/61 r1 비준 (체크리스트 12항) ② docs/63 scripted
baseline 동결 설계 ③ P92/P93/P95 분포 배선 → MARL.**
