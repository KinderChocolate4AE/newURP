# 세션 핸드오프 2026-08-07 — 스케일 v2 확립, 다음 = 위협 계약 v3 설계

너는 `newURP` 를 이어서 작업하는 **검증 우선 연구 엔지니어**다. 이 문서는
2026-08-06~07 두 세션(계약 개정 R1/R2 → 리뷰 3·4 → recoverability 사슬 →
★스케일 v2 피봇)의 승계본이다. 이전 핸드오프(`HANDOFF_2026-08-06.md`)의
규율은 전부 유효하며, 아래가 그 위에 누적된다.

---

## 0. 저장소·브랜치

```
로컬  C:\Users\Teemo\Desktop\ANDES\URP\newURP     서버  ~/newURP
```

| 브랜치 | 상태 | 용도 |
|---|---|---|
| **`feat/scale-up-v2`** | ★ 활성 (e3d2478+, push 됨) | 스케일 v2 격리. **여기서 계속** |
| `feat/l2-mappo-train` | 동결 (리뷰 4 사이클 마감까지 push) | legacy 라인. v2 안정 후 병합 판단 |

시작 절차: `git status` · `git log -8 --oneline` · `pytest -q --ignore=tests/test_fire_audit.py`
(로컬은 torch 미설치라 60 skip 이 정상 — memory `local-env-quirks` 참조).

## 1. 읽기 순서

```
이 핸드오프
docs/59_scale_v2_prereg.md      ★ 현행 계약 (스케일 v2 + 정정 이력 + 위협 v3 축)
docs/58_review4_verdicts.md     리뷰 4 판정 + P2' + pre-fire arm + P84
docs/57, docs/56, docs/54       NK 의미 감사 · recoverability probe · R1/R2 계약
HANDOFF_2026-08-06.md           원 규율 (오류 1~17, 성공 술어, 3-way 규율)
```

## 2. ★ 이 이틀의 핵심 서사 (되돌리지 말 것)

```
R1(접촉 resolver)·R2(miss handoff) 계약 복구 -- 기본 off 비트동일, 리뷰 3 통과
→ recoverability 사슬: 2×2 → latest-start sweep → P2'(NK-aware) → pre-fire
   · 접촉 42/42 NK-안 (legacy) · pre-fire ep35 = 캠페인 최초 NK-밖 무력화
   · P84: 접촉반경(0.75) 밖 limiter 는 공격자에 인과효과 0 (42/42) -- 현 A2 에서
     shepherding 검정 불가 (artifact 고정)
→ ★ 궤적 뷰어(Hyunjun 지시)가 5분 만에 지배 변수 발견: legacy 스케일
   (스폰 21~25 m·교전 0.75~2.8 s·ring→NK 0.1 s)이 비준된 적 없는 M2 세습
→ 기존 결과 전부 "legacy small-scale regime" 재스코프 (폐기 아님)
→ 스케일 v2 (docs/59, 격리 브랜치): 스폰 300 m·episode 800(40 s)·ring x=50,
   하드웨어/안전 절대값(kill_radius·cone·tau_deploy·r_nk·dt) 고정.
   P85 비트동일 + P86 스모크 green
→ V4-800 (n=100): 포획 15/침투 85/절단 0. legacy(16/84)와 근사
   = 22배 커진 대응시간을 아무도 안 씀 (fire 22/100·hold·등속 공격자)
→ V5 육안 실증: ① ep1 NK-밖 자연 fallback 창 (miss 후 회피 오버슛 반전,
   1.25 m/s @ d_asset 16.1 m, ~0.45 s) -- legacy oracle 사슬이 못 찾던 창이
   baseline 에 실존, 잡을 주체 미구현 ② ep94 무발사 = v_soft 천장 0.728
   (hold limiter 미커버 -- 발사 계약 정상, limiter 부동이 원인)
```

**v2 의 의미**: 대응시간·fallback 창·발사 게이트 셋 다 "limiter 가 움직여야
열리는" 구조 = 원 연구 질문(협력 기동)의 무대가 처음 생겼다.

## 3. 다음 세션의 유일한 작업 — 위협 계약 v3 설계 (docs/59 §4)

Hyunjun 지시 누적 3축 (**설계 문서 먼저 → 비준 → 배선**. 결과 전 커밋):

```
(i)   종축 속도 전략 -- 등속 순항 탈피 (급가속 터미널 스프린트·감속 페인트).
      능력 v_max = 1.5×att_speed 는 기 선언(m4_config CAPABILITY_RATIOS),
      행동만 미구현
(ii)  횡 반응성 -- limiter 회피 (P84 로 현 A2 부재 확정. 감지 반경 > 교전
      반경 + manipulation check 통과 의무)
(iii) 스폰 불규칙성 -- 거리 브래킷 확장 + 접근 방위 랜덤화.
      ★ 파급: "회랑" 프레임 해체 -- ring 축-고정(50,0,0) 가정 붕괴.
      대응 = 표적 중심 전방위 배치 or 탐지→방위 식별→재배치를 문제에 포함
      (후자가 mode scheduling 연구 질문의 자연 무대)
```

경로 A/B(interception-coverage 축소 vs shepherding 유지)와 **한 계약으로**
묶어 결정. 기존 A2 는 보존하고 opt-in variant 로 (스폰 랜덤화 SpawnSpec
패턴과 동일 규율). **금지**: v3 확정 전 limiter 학습·oracle 확장·r_nk 튜닝.

## 4. 신규 오류 패턴 (기존 1~17 에 추가 — 이번 이틀 실제 발생분)

```
18. 선언값을 추정하지 말고 레지스트리를 읽는다 -- 지평선 400→480→800 2회
    정정. 480 은 att_speed 하한을 ~14 로 추정 (실제 브래킷 (8,30), m4_config:183)
19. 분포 요약을 행동으로 과잉해석 금지 -- TRUNCATED min_d 중앙 64.7 을
    "A2 배회"로 오독. 실제 = 저속 draw 의 지평선 부족 (ep4 는 포획 진행 중 절단)
20. ★ 새 regime·이상 분포·설명 안 되는 결과 = 수치 탐색 전에 뷰어 먼저
    (memory viz-first-policy). 성공/실패 대조 판을 같이 렌더
21. 30분+ 실험 = 랩 서버 tmux+ntfy·병렬 샤딩·진행 로그·incremental 저장
    (memory long-run-policy). 로컬 직렬 5.5h 사고 재발 금지
22. "과대 표현" 리뷰 패턴: primary 수치 실패와 기전 귀속 성공은 두 줄로 분리
    보고, budget 한정 결과에 "불가능/환원/닫힘" 단정 금지 (리뷰 3·4 판정)
```

## 5. 도구 인벤토리 (전부 v2 이식 완료·테스트 green)

```
shepherd/scale_v2.py                 v2 config overlay (SCALE_V2_CFG·SPAWN)
shepherd/scripts/scale_v2_baseline.py  V4 baseline (--eps 샤딩)
shepherd/scripts/dump_trajectory.py    궤적 덤프 (--v2 --eps · --from-json 분업)
viz/trajectory_viewer_template.html    뷰어 (cone·net 전개 비행·m 격자·타임라인)
shepherd/scripts/recoverability_probe.py  CEM planner·2×2·sweep·P2'·prefire
shepherd/scripts/coupling_gate.py      P84 manipulation check
tests/: test_scale_v2(P85/86) · test_recoverability_probe(P83×8) ·
        test_contact_resolver(P78~80+반경키) · test_net_miss_handoff(P81/82)
```

뷰어 artifact: legacy = claude.ai/code/artifact/650b745b… · v2 = …/d0cb484f…
(재게시 = 같은 파일 경로 republish. 서버 재시뮬 → JSON 커밋 → 로컬 --from-json)

## 6. 운영 원칙 (memory 에도 등재됨 — 자동 적용)

- **viz-first** · **long-run 서버 정책** · **Notion Daily Log 자동 기록**
  (collection://e4d1f298-…, 판정형 제목, 세션 마감마다. 오늘치 기록 완료)
- 사전등록 규율 불변: 계약/판정식은 결과 이전 커밋, 결과 후 불변경, 이탈 시
  선언된 축으로 분해. 리뷰 프롬프트(자기완결·적대적) 사이클 유지
- 이종/비대칭 용어, "물리적 불가능" 등 금지 표현: HANDOFF_2026-08-06 §13 그대로
```
